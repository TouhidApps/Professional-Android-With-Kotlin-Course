<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="300dp">

    <TextView
        android:id="@+id/tvTitle"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_marginBottom="3dp"
        android:layout_width="match_parent"
        android:lines="1"
        tools:text="Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, "
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/tvBody"
        style="@style/TextAppearance.Compat.Notification.Line2"
        android:layout_width="match_parent"
        android:maxLines="4"
        tools:text="Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI, Small notification UI,  Small notification UI, Small notification UI, Small notification UI, "
        android:layout_height="wrap_content"/>

    <ImageView
        android:id="@+id/ivThumb"
        android:src="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:layout_height="0dp"/>

</LinearLayout>